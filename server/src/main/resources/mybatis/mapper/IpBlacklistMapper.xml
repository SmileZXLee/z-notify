<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zxlee.znotifyapi.mapper.IpBlacklistMapper">

    <resultMap id="BaseResultMap" type="cn.zxlee.znotifyapi.pojo.po.IpBlacklistPO">
        <id column="id" jdbcType="VARCHAR" property="id" />
        <result column="ip" jdbcType="VARCHAR" property="ip" />
        <result column="createtime" jdbcType="TIMESTAMP" property="createtime" />
        <result column="updatetime" jdbcType="TIMESTAMP" property="updatetime" />
    </resultMap>

    <sql id="Base_Column_List">
        id, ip, createtime, updatetime
    </sql>

    <select id="getByIp" parameterType="java.lang.String" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from t_ip_blacklist
        where ip = #{ip,jdbcType=VARCHAR}
    </select>

    <select id="list" parameterType="java.util.Map" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from t_ip_blacklist
        order by createtime desc
    </select>

    <insert id="insertOne" parameterType="cn.zxlee.znotifyapi.pojo.po.IpBlacklistPO">
        insert into t_ip_blacklist (id, ip, createtime, updatetime)
        values (#{id,jdbcType=VARCHAR}, #{ip,jdbcType=VARCHAR},
                #{createtime,jdbcType=TIMESTAMP}, #{updatetime,jdbcType=TIMESTAMP})
    </insert>

    <delete id="deleteById" parameterType="java.lang.String">
        delete from t_ip_blacklist
        where id = #{id,jdbcType=VARCHAR}
    </delete>

</mapper>
